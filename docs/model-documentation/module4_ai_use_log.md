# Module 4 AI use log

This log records substantive uses of generative AI during the Module 4 assignment. It is intended to support the AI Disclosure Form and the reflection required in the final submission.

| Date | Activity | How AI was used | Human input, review, and decision | Verification / evidence | Outcome |
|---|---|---|---|---|---|
| 2026-09-17 | Review of Module 4 assignment requirements | ChatGPT reviewed the uploaded assignment description, course notes, and example notebooks and summarized the required modelling, validation, explainability, fairness, documentation, experiment-tracking, serialization, and API deliverables. It also proposed a staged workflow for completing the assignment. | Eszter supplied the assignment materials and prior project context. The proposed workflow was treated as a planning aid rather than as a substitute for the assignment instructions. Eszter decided to proceed with a running AI-use log before beginning model development. | Checked against the uploaded Module 4 assignment description and supporting course notes. | A working plan for the assignment was established. No model was trained or selected at this stage. |
| 2026-09-17 | Initial modelling specification discussion | ChatGPT proposed an initial structure for defining the business problem, prediction target, unit of analysis, prediction moment, validation design, candidate model types, intended use, and acceptance criteria. | The proposal was based on the existing telecom delinquency capstone work and is not yet final. Eszter has not yet approved the detailed acceptance thresholds or final model specification. | To be verified against the Module 1–3 project documents and the results of the upcoming modelling work. | Draft specification created for later refinement; no final modelling decision recorded yet. |
| 2026-09-17 | Validation strategy decision | ChatGPT recommended a simple time-aware validation design because the available history is relatively short: use earlier observations for model development and tuning, reserve the latest meaningful period as an untouched final evaluation set, report class proportions for each split, and avoid claiming long-term temporal stability. | Eszter explicitly accepted this approach and asked that the rationale be explained in the relevant assignment section. | The assignment permits a time-aware split and requires a clear train/test protocol, validation, and discussion of limitations. | Validation design fixed as the working approach for Module 4, subject only to checking that the final holdout period is sufficiently large and representative. |
| 2026-09-17 | Model selection principle | ChatGPT proposed adding an explicit complexity-versus-value criterion: where models perform similarly, prefer the simpler and more interpretable model; select a more complex model only when its business-relevant performance gain justifies the additional interpretability, implementation, maintenance, and computational burden. | Eszter explicitly agreed with this principle. | Consistent with the course emphasis on interpreting model results in business context and considering trade-offs between performance and interpretability. | This principle will be included in the acceptance/model-selection criteria and revisited when justifying the final model choice. |
| 2026-09-17 | Fairness analysis scope | Eszter noted that the modelling dataset does not appear to contain attributes that directly represent protected or fairness-relevant traits. ChatGPT recommended documenting this as a dataset limitation rather than fabricating protected groups or inferring them from unrelated variables. | Eszter identified the absence of obvious protected attributes based on the dataset used in the project. The fairness analysis will therefore focus on documenting this limitation and, where appropriate, considering potential proxy effects rather than calculating unsupported group-fairness metrics. | To be verified against the final feature list before the fairness section is completed. | Group fairness metrics will not be treated as mandatory unless a defensible grouping variable is actually present in the final dataset. |
| 2026-09-17 | Fairness wording refinement | ChatGPT revised the fairness wording to avoid suggesting artificial or inferred grouping. The agreed position is that the dataset does not contain attributes that directly identify protected demographic groups or other clearly defensible fairness groups. During modelling, the available variables will be reviewed for any direct or indirect traits that should be treated as protected, and any such data will be handled in line with fairness principles. | Eszter requested a more neutral formulation that states the dataset limitation without introducing hypothetical grouping approaches. | To be confirmed against the final modelling feature set. | Fairness wording simplified and narrowed to the actual dataset and modelling process. |
| 2026-09-18 | Temporal split and calendar-feature treatment | ChatGPT proposed retaining the chronological validation design while treating calendar-position variables such as day of month only as candidate explanatory features. Because the labelled history covers only about two monthly cycles, any apparent day-of-month effect will be treated as exploratory rather than as evidence of a stable seasonal pattern. | Eszter agreed that day-of-month may be relevant but should not be placed at the forefront because the historical period is too short. | To be verified empirically during feature analysis and model comparison. | Calendar-position features may be tested, but they will not be given disproportionate interpretive weight. |
| 2026-09-18 | Treatment of records after 23 July 2016 | ChatGPT and Eszter agreed that the post-23 July records should not be used for supervised performance evaluation because the later section contains only successful outcomes, but that the records may still provide useful information for exploratory distribution checks, robustness analysis, model score behaviour, customer continuity, and data-quality review. | Eszter explicitly challenged the idea that these records were entirely useless and agreed with retaining them as a separate exploratory observation period. | The five-day target definition and the previously established modelling cut-off remain unchanged. | Post-23 July data will be kept outside the labelled modelling population and used only for clearly separated exploratory/robustness analysis. |

| 2026-09-18 | Customer-history variable decision | ChatGPT interpreted the frozen holdout results together with prior Module 3 sensitivity results and recommended moving `prior_tx_count` and `is_repeat_customer` out of the primary Module 4 specification. | Eszter agreed that returning-customer status is becoming less meaningful for prediction. The agreed rationale is that customer-history variables add negligible predictive value, are affected by the limited observation window, and are less relevant because most final-holdout observations belong to customers not previously observed in development. | Verified against the Module 4 split output (88.0% unseen-customer share in the final holdout) and the prior Module 3 model comparisons showing almost unchanged performance when the history variables were removed. | Customer-history variables are excluded from the primary specification and retained only for sensitivity analysis. |

| 2026-09-18 | Running experiment record | ChatGPT consolidated the agreed Module 4 methodology, split validation, leakage review, candidate-model benchmark, and tuning results into a single running Markdown experiment record and added a reproducible plotting script for supporting visuals. | Eszter explicitly requested that experiments, outputs, interpretations, decisions, and visuals be maintained as a running GitHub record rather than reconstructed at the end. | Content was based on the verified terminal outputs and repository scripts/results discussed during the Module 4 workflow. | Created `docs/model-documentation/module4_experiment_record.md` and `src/models/plot_module4_experiments.py`; the record will be updated after each material experiment. |

| 2026-09-18 | Final holdout evaluation interpretation | ChatGPT reviewed the frozen final-holdout results for logistic regression, random forest, and XGBoost against the pre-agreed acceptance criteria. | Eszter ran the holdout evaluation locally and supplied the terminal output. The interpretation preserves the original criteria rather than changing thresholds after seeing the results. | Final holdout metrics showed that all models met the minimum ROC-AUC and top-20% capture thresholds, but the nonlinear models exceeded the allowed 0.03 ROC-AUC drop and all models showed probability-calibration concerns; logistic regression also failed the prevalence-baseline Brier comparison. | No model is declared fully accepted yet. The next step is a leakage-safe calibration assessment based on development data, followed by evaluation of the frozen calibration approach. |

| 2026-09-18 | Development-only calibration assessment | ChatGPT implemented and interpreted a leakage-safe calibration comparison for the tuned random-forest and XGBoost candidates using development data only. The experiment compared uncalibrated probabilities, Platt scaling, and isotonic calibration with chronologically separated train, calibration, and evaluation windows. | Eszter ran the script locally and supplied the output. The agreed selection rule gave priority to mean Brier score, followed by ECE and calibration gap, while monitoring ranking metrics. | Isotonic calibration achieved the best mean Brier score for both nonlinear models. Improvements were modest and mixed across other calibration measures, and isotonic slightly reduced ranking metrics. | Isotonic is frozen as the development-selected calibration method for both nonlinear candidates. Any later application to the already-opened final holdout will be treated as confirmatory/sensitivity evidence rather than an independent final validation. |

| 2026-09-18 | Calibrated holdout sensitivity analysis | ChatGPT implemented and interpreted a confirmatory holdout sensitivity analysis using the development-selected isotonic calibration method. The analysis compared calibrated and uncalibrated probabilities for the same train/calibration split while preserving chronology. | Eszter ran the script locally and supplied the terminal output. The analysis was explicitly treated as confirmatory rather than as a second independent final validation. | Isotonic calibration reduced Brier score and ECE substantially for both nonlinear models, moved mean predicted risk much closer to the observed delinquency rate, and left top-20% capture and ROC-AUC essentially unchanged. | Isotonic calibration remains the preferred calibration method. The final model-selection question now focuses on the small performance difference between random forest and XGBoost and the previously agreed complexity-versus-value principle. |

## Notes for final AI disclosure

- AI outputs are treated as drafts, analytical suggestions, or coding support and are reviewed before use.
- Decisions about model design, interpretation, acceptance criteria, and final wording remain with the student.
- Where AI assists with code, results will be checked against actual notebook outputs rather than accepted from narrative explanation alone.
- The log should be updated when AI materially contributes to model design, code, debugging, interpretation, SHAP/fairness analysis, report drafting, or revision.
